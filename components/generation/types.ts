/** Re-export canonical DTOs from `lib/types` for feature-local imports. */
export type {
  GenerationType,
  SessionItem,
  OutputItem,
  GenerationItem,
  ModelItem,
} from "@/lib/types/generation";
